# 🚨 إصلاح عاجل: Check Constraint للحالات

## ❌ **الخطأ:**
```
Error updating project: Error: new row for relation "projects" violates check constraint "projects_status_check"
```

## 🔍 **السبب:**
قاعدة البيانات تحتوي على **check constraint** يحد من قيم `status`، والحالات الجديدة غير مسموح بها!

---

## ✅ **الحل:**

### 🎯 **نفّذ هذا الكود في Supabase SQL Editor:**

```sql
-- 1️⃣ إزالة الـ constraint القديم
ALTER TABLE projects 
DROP CONSTRAINT IF EXISTS projects_status_check;

-- 2️⃣ إضافة constraint جديد بجميع الحالات
ALTER TABLE projects
ADD CONSTRAINT projects_status_check 
CHECK (status IN (
  'جاري العمل',
  'منجز',
  'متأخر',
  'متقدم',
  'متعثر',
  'متوقف',
  'تم الاستلام الابتدائي',
  'تم الرفع بالاستلام الابتدائي',
  'تم الرفع بالاستلام النهائي',
  'تم الاستلام النهائي'
));
```

---

## 📝 **خطوات التنفيذ:**

1. **افتح Supabase Dashboard** → **SQL Editor**
2. **انسخ الكود أعلاه**
3. **الصقه واضغط Run** (أو F5)
4. **انتظر رسالة Success ✅**

---

## ✅ **بعد التنفيذ:**

- ✅ جميع الحالات الـ 10 ستعمل بنجاح
- ✅ لن يظهر خطأ check constraint مرة أخرى
- ✅ التعديل على المشاريع سيعمل بدون مشاكل

---

## 🎯 **اختبر:**

1. **عدّل أي مشروع**
2. **غيّر الحالة** إلى "تم الاستلام الابتدائي" أو "تم الرفع بالاستلام النهائي"
3. **احفظ** ← يجب أن يعمل بنجاح! ✅

---

## 📞 **بعد التنفيذ:**
أخبرني: **"تم التنفيذ ✅"** وسأنتقل للمشكلة 3! 🚀
