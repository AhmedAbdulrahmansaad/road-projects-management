# 🗄️ دليل إعداد قاعدة البيانات - خطوة بخطوة

## ⚠️ المشكلة التي واجهتها:

عند محاولة إنشاء حساب، السيرفر يرجع **500 Internal Server Error** لأن جداول قاعدة البيانات غير موجودة!

```
❌ Signup error from server: {error: 'خطأ في إنشاء الحساب'}
```

---

## ✅ الحل (5 دقائق فقط!):

### **خطوة 1: افتح Supabase Dashboard**

اذهب إلى: **https://supabase.com/dashboard**

1. اضغط على مشروعك: **`lreziibjjeaeirgeszkx`**
2. من القائمة الجانبية → اضغط **"SQL Editor"**

---

### **خطوة 2: افتح محرر SQL جديد**

في صفحة SQL Editor:
1. اضغط على زر **"New query"** (أعلى اليسار)
2. سيفتح لك صفحة فارغة

---

### **خطوة 3: انسخ كود SQL الكامل**

1. افتح الملف: **`/COMPLETE_DATABASE_SETUP.sql`** (في Figma Make)
2. **انسخ المحتوى كله!** (Ctrl+A ثم Ctrl+C)
3. الصق في محرر SQL في Supabase

**أو** انسخ من هنا مباشرة:

```sql
-- انسخ كل محتوى ملف COMPLETE_DATABASE_SETUP.sql
-- الموجود في المشروع
```

---

### **خطوة 4: شغّل الكود!**

1. اضغط زر **"Run"** (أسفل اليمين)
2. انتظر 3-5 ثواني
3. يجب أن تشوف رسالة: **"Success. No rows returned"** ✅

---

### **خطوة 5: تحقق من الجداول**

1. من القائمة الجانبية → **"Table Editor"**
2. يجب أن تشوف **6 جداول جديدة**:
   - ✅ `users`
   - ✅ `projects`
   - ✅ `daily_reports`
   - ✅ `percentage_statements`
   - ✅ `performance_contracts`
   - ✅ `notifications`

**إذا شفتها = تمام! ✅**

---

## 🧪 اختبر الآن!

### **ارجع للتطبيق:**

1. اعمل **Refresh** (Ctrl + Shift + R)
2. اذهب لصفحة **"إنشاء حساب"**
3. املأ البيانات:
   ```
   الاسم: أحمد السعودي
   الإيميل: ahmad@test.com
   الرمز: 123456
   الدور: المهندس
   ```
4. اضغط **"إنشاء حساب"**

---

## ✅ النتيجة المتوقعة:

يجب أن تشوف:
1. ✅ رسالة نجاح: **"تم إنشاء الحساب بنجاح"**
2. ✅ يسجل دخول تلقائياً
3. ✅ ينتقل لـ **Dashboard**
4. ✅ اسمك يظهر في الأعلى: **"أحمد السعودي"**

---

## 🔍 تحقق من Console:

في F12 → Console، يجب أن تشوف:

```
🔵 Starting signup process...
🔵 Signup response status: 200  ← تغيرت من 500!
✅ Signup successful, attempting auto sign-in...
✅ Auto sign-in successful!
🔍 App: User state changed: { user: "ahmad@test.com" }
```

---

## ❌ إذا ما اشتغل:

### **تحقق 1: هل الجداول موجودة؟**

في Supabase → **Table Editor**:
- يجب أن تشوف 6 جداول
- جدول `users` يحتوي على Columns: `id`, `email`, `password`, `name`, `role`

---

### **تحقق 2: هل RLS مفعّل؟**

في Supabase → **Table Editor** → اضغط على جدول `users`:
- اضغط على أيقونة **"RLS"** (أعلى اليمين)
- يجب أن تشوف: **"Row Level Security is enabled"** ✅
- Policy واحد: **"Enable all for service role"** ✅

---

### **تحقق 3: هل السيرفر يعمل؟**

في Supabase → **Edge Functions**:
- اضغط على **"make-server-a52c947c"** (أو اسم مشابه)
- شيك **Logs** → يجب أن تشوف طلبات POST للـ `/signup`

---

## 💡 ملاحظات مهمة:

### **1. لماذا حدثت المشكلة؟**

لأن ملفات SQL في `/sql-scripts/` **لا تُنفذ تلقائياً!**

يجب نسخها ولصقها في Supabase Dashboard يدوياً.

---

### **2. هل أحتاج لتكرار هذه الخطوة؟**

**لا!** فقط مرة واحدة. بعد إنشاء الجداول، ستبقى موجودة للأبد.

---

### **3. ماذا لو أخطأت؟**

لا تقلق! الكود يحتوي على:
```sql
DROP TABLE IF EXISTS ... CASCADE;
```

يعني لو شغّلته مرة ثانية، سيحذف الجداول القديمة ويعيد إنشاءها.

---

### **4. هل سيؤثر على المستخدمين الحاليين؟**

**نعم!** إذا كان عندك مستخدمين حاليين، سيتم حذفهم.

لكن ما عندك مستخدمين حالياً (لأن الـ Sign Up ما كان يشتغل 😅)

---

## 📸 إذا ما اشتغل:

صور لي:
1. **Table Editor** في Supabase (كل الجداول)
2. **Console** في التطبيق (بعد محاولة Sign Up)
3. **SQL Editor** (نتيجة تشغيل الكود)

**وأنا أساعدك!** 🚀

---

## 🎯 ما بعد الإعداد:

بعد ما تخلص، جرب:

### **1. إنشاء حسابات متعددة:**
- المدير العام: `ceo@test.com`
- مدير فرع: `branch@test.com`
- مهندس: `engineer@test.com`

### **2. إنشاء مشروع:**
- من Dashboard → إدارة المشاريع
- اضغط "إضافة مشروع"
- املأ البيانات واحفظ

### **3. اختبر المساعد الذكي:**
- اذهب لصفحة المساعد الذكي
- اسأله: **"كم عدد المشاريع؟"**
- يجب أن يرد بالعدد الصحيح!

---

## 🇸🇦 نصيحة:

بعد إعداد قاعدة البيانات، النظام **يشتغل 100% في Figma Make!**

**ما تحتاج ترفعه على Vercel** إلا إذا تبغى تنشره للاستخدام الفعلي.

---

## ✅ خلاص!

**الآن روح سوي الخطوات وخبرني!** 💪🚀😊

**أي مشكلة = صور لي وأنا أحلها فوراً!**
