# 🔧 إصلاح Check Constraint للحالات (Status)

## ⚠️ المشكلة:

القيم المُستخدمة في الكود **لا تتطابق** مع القيم المسموح بها في قاعدة البيانات!

### **القيم في الكود (CreateProject.tsx):**
```typescript
'جاري العمل'
'منجز'
'متأخر'
'متقدم'
'متعثر'
'متوقف'
'تم الرفع بالاستلام الابتدائي'
'تم الاستلام النهائي'
```

### **القيم في قاعدة البيانات (projects table):**
```sql
'قيد التنفيذ'
'مكتمل'
'متوقف'
'In Progress'
'Completed'
'Paused'
```

---

## ✅ الحل:

### **خطوة واحدة فقط - قم بتنفيذها في Supabase SQL Editor:**

انتقل إلى:
```
Supabase Dashboard → SQL Editor → New Query
```

**الصق هذا الكود واضغط RUN:**

```sql
-- 🔧 إصلاح Check Constraint لجدول Projects
-- تحديث القيم لتطابق الكود

-- 1. حذف الـ constraint القديم
ALTER TABLE projects 
DROP CONSTRAINT IF EXISTS projects_status_check;

-- 2. إضافة constraint جديد بالقيم الصحيحة
ALTER TABLE projects 
ADD CONSTRAINT projects_status_check 
CHECK (status IN (
  'جاري العمل',
  'منجز',
  'متأخر',
  'متقدم',
  'متعثر',
  'متوقف',
  'تم الرفع بالاستلام الابتدائي',
  'تم الاستلام النهائي'
));

-- 3. تحديث أي بيانات موجودة (إن وجدت) لتطابق القيم الجديدة
UPDATE projects 
SET status = CASE 
  WHEN status = 'قيد التنفيذ' THEN 'جاري العمل'
  WHEN status = 'مكتمل' THEN 'منجز'
  WHEN status = 'In Progress' THEN 'جاري العمل'
  WHEN status = 'Completed' THEN 'منجز'
  WHEN status = 'Paused' THEN 'متوقف'
  ELSE status
END
WHERE status IN ('قيد التنفيذ', 'مكتمل', 'In Progress', 'Completed', 'Paused');

-- ✅ تم! تحقق من النتيجة
SELECT DISTINCT status FROM projects;
```

---

## 🧪 بعد التنفيذ - اختبر:

### **1. أعد تحميل الصفحة**

### **2. حاول إنشاء مشروع جديد:**

```
وصف أمر العمل: مشروع اختبار الحالة
رقم أمر العمل: 12345
... املأ باقي الحقول
الحالة: جاري العمل  ← اختبر هذا
```

### **3. يجب أن ينجح بدون أخطاء!** ✅

---

## 📋 القيم المسموح بها الآن:

| القيمة العربية | الوصف |
|----------------|--------|
| `جاري العمل` | المشروع قيد التنفيذ |
| `منجز` | المشروع منتهي |
| `متأخر` | المشروع متأخر عن الجدول |
| `متقدم` | المشروع متقدم عن الجدول |
| `متعثر` | المشروع يواجه مشاكل |
| `متوقف` | المشروع متوقف مؤقتاً |
| `تم الرفع بالاستلام الابتدائي` | تم رفع الاستلام الابتدائي |
| `تم الاستلام النهائي` | تم الاستلام النهائي |

---

## ✅ بعد التنفيذ:

**ستتمكن من:**
- ✅ إنشاء مشاريع جديدة
- ✅ تحديث حالة المشاريع
- ✅ عرض التقارير بشكل صحيح

---

**الآن: نفذ الـ SQL أعلاه في Supabase!** 🚀💪🇸🇦
